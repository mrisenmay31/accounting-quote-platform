<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ledgerly Investment Card - High Conversion</title>
  <style>
    body {
      margin: 0;
      padding: 40px 20px;
      background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Inter', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .investment-card {
      background: linear-gradient(135deg, #ffffff 0%, #f8fdf9 100%);
      border: 2px solid #d1fae5;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(16, 185, 129, 0.15),
                  0 8px 24px rgba(45, 106, 79, 0.08);
      padding: 0;
      max-width: 440px;
      width: 100%;
      overflow: hidden;
      position: relative;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .investment-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 24px 70px rgba(16, 185, 129, 0.2),
                  0 12px 32px rgba(45, 106, 79, 0.12);
    }

    /* Subtle animated gradient accent */
    .card-glow {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, #10b981, #059669, #047857, #059669, #10b981);
      background-size: 200% 100%;
      animation: shimmer 3s linear infinite;
    }

    @keyframes shimmer {
      0% { background-position: -200% 0; }
      100% { background-position: 200% 0; }
    }

    .card-header {
      padding: 28px 28px 20px;
      text-align: center;
      position: relative;
    }

    .card-label {
      font-size: 12px;
      font-weight: 700;
      color: #059669;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
    }

    .pulse-dot {
      width: 8px;
      height: 8px;
      background: #10b981;
      border-radius: 50%;
      animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.5; transform: scale(1.1); }
    }

    .card-title {
      font-size: 26px;
      font-weight: 800;
      color: #0f172a;
      margin: 0 0 8px;
      letter-spacing: -0.03em;
      line-height: 1.2;
    }

    .value-subtitle {
      font-size: 14px;
      color: #64748b;
      font-weight: 500;
    }

    /* Price Section with Hierarchy */
    .pricing-hero {
      padding: 24px 28px 28px;
      background: linear-gradient(135deg, #f0fdf4 0%, #ffffff 50%, #f0fdf4 100%);
      border-top: 1px solid #d1fae5;
      border-bottom: 1px solid #d1fae5;
      position: relative;
      overflow: hidden;
    }

    .pricing-hero::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -20%;
      width: 300px;
      height: 300px;
      background: radial-gradient(circle, rgba(16, 185, 129, 0.08) 0%, transparent 70%);
      border-radius: 50%;
    }

    .primary-price {
      text-align: center;
      margin-bottom: 20px;
      position: relative;
      z-index: 1;
    }

    .price-tag {
      font-size: 13px;
      font-weight: 600;
      color: #64748b;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-bottom: 8px;
    }

    .big-amount {
      display: flex;
      align-items: baseline;
      justify-content: center;
      gap: 6px;
      margin-bottom: 12px;
      transition: transform 0.3s ease;
    }

    .investment-card:hover .big-amount {
      transform: scale(1.03);
    }

    .currency-symbol {
      font-size: 36px;
      font-weight: 800;
      background: linear-gradient(135deg, #059669 0%, #10b981 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .main-price {
      font-size: 64px;
      font-weight: 900;
      line-height: 1;
      background: linear-gradient(135deg, #047857 0%, #059669 50%, #10b981 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      letter-spacing: -0.04em;
      text-shadow: 0 4px 12px rgba(5, 150, 105, 0.15);
    }

    .period {
      font-size: 18px;
      font-weight: 600;
      color: #64748b;
    }

    .secondary-price {
      text-align: center;
      font-size: 15px;
      color: #475569;
      font-weight: 500;
      position: relative;
      z-index: 1;
    }

    .secondary-price strong {
      color: #0f172a;
      font-weight: 700;
    }

    /* ROI Value Bar - The Game Changer */
    .roi-bar {
      margin: 24px 28px;
      padding: 16px 20px;
      background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
      border: 2px solid #fbbf24;
      border-radius: 12px;
      position: relative;
      overflow: hidden;
      animation: glow 2s ease-in-out infinite;
    }

    @keyframes glow {
      0%, 100% { box-shadow: 0 0 20px rgba(251, 191, 36, 0.3); }
      50% { box-shadow: 0 0 30px rgba(251, 191, 36, 0.5); }
    }

    .roi-bar::before {
      content: '✨';
      position: absolute;
      top: 12px;
      left: 12px;
      font-size: 20px;
      animation: sparkle 1.5s ease-in-out infinite;
    }

    @keyframes sparkle {
      0%, 100% { opacity: 1; transform: rotate(0deg) scale(1); }
      50% { opacity: 0.7; transform: rotate(20deg) scale(1.1); }
    }

    .roi-title {
      font-size: 12px;
      font-weight: 700;
      color: #92400e;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-bottom: 6px;
    }

    .roi-value {
      font-size: 22px;
      font-weight: 900;
      color: #78350f;
      letter-spacing: -0.02em;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }

    .roi-icon {
      font-size: 24px;
    }

    .payback-period {
      margin-top: 8px;
      font-size: 13px;
      color: #92400e;
      font-weight: 600;
      text-align: center;
    }

    /* Trust Badges with Icons */
    .trust-section {
      padding: 24px 28px 28px;
      background: white;
    }

    .trust-label {
      font-size: 11px;
      font-weight: 700;
      color: #64748b;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      margin-bottom: 16px;
      text-align: center;
    }

    .trust-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
    }

    .trust-badge {
      display: flex;
      align-items: center;
      gap: 8px;
      background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
      border: 1.5px solid #e2e8f0;
      border-radius: 10px;
      padding: 10px 12px;
      font-size: 12px;
      color: #334155;
      font-weight: 600;
      transition: all 0.2s ease;
      cursor: default;
      line-height: 1.3;
    }

    .trust-badge:hover {
      background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
      border-color: #86efac;
      transform: translateY(-2px) scale(1.02);
      box-shadow: 0 4px 12px rgba(5, 150, 105, 0.15);
    }

    .badge-icon {
      flex-shrink: 0;
      width: 20px;
      height: 20px;
      color: #059669;
    }

    .badge-text {
      flex: 1;
      line-height: 1.2;
    }

    .badge-highlight {
      color: #059669;
      font-weight: 800;
      font-size: 14px;
    }

    /* CTA Section */
    .cta-section {
      padding: 24px 28px 28px;
      background: white;
      border-top: 1px solid #f1f5f9;
    }

    .cta-button {
      width: 100%;
      background: linear-gradient(135deg, #059669 0%, #10b981 100%);
      color: white;
      border: none;
      padding: 18px 28px;
      border-radius: 12px;
      font-size: 17px;
      font-weight: 800;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 8px 24px rgba(5, 150, 105, 0.35);
      letter-spacing: 0.02em;
      position: relative;
      overflow: hidden;
    }

    .cta-button::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
      transition: left 0.5s ease;
    }

    .cta-button:hover::before {
      left: 100%;
    }

    .cta-button:hover {
      background: linear-gradient(135deg, #047857 0%, #059669 100%);
      transform: translateY(-3px);
      box-shadow: 0 16px 40px rgba(5, 150, 105, 0.45);
    }

    .cta-button:active {
      transform: translateY(-1px);
    }

    .button-text {
      position: relative;
      z-index: 1;
    }

    .button-arrow {
      transition: transform 0.3s ease;
      position: relative;
      z-index: 1;
    }

    .cta-button:hover .button-arrow {
      transform: translateX(6px);
    }

    .urgency-text {
      margin-top: 14px;
      text-align: center;
      font-size: 12px;
      color: #64748b;
      font-weight: 500;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
    }

    .urgency-icon {
      color: #f59e0b;
      font-size: 14px;
    }

    .disclaimer {
      margin-top: 16px;
      font-size: 11px;
      color: #94a3b8;
      text-align: center;
      font-style: italic;
      line-height: 1.4;
    }

    /* Responsive */
    @media (max-width: 480px) {
      .main-price {
        font-size: 52px;
      }

      .card-header,
      .pricing-hero,
      .trust-section,
      .cta-section {
        padding-left: 20px;
        padding-right: 20px;
      }

      .roi-bar {
        margin-left: 20px;
        margin-right: 20px;
      }

      .trust-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="investment-card">
    <div class="card-glow"></div>
    
    <div class="card-header">
      <div class="card-label">
        <span class="pulse-dot"></span>
        Your Investment
      </div>
      <h2 class="card-title">Total Estimated Investment</h2>
      <p class="value-subtitle">Premium tax advisory partnership</p>
    </div>

    <div class="pricing-hero">
      <div class="primary-price">
        <div class="price-tag">Monthly Partnership</div>
        <div class="big-amount">
          <span class="currency-symbol">$</span>
          <span class="main-price">1,250</span>
          <span class="period">/mo</span>
        </div>
      </div>

      <div class="secondary-price">
        + <strong>$150</strong> one-time setup
      </div>
    </div>

    <div class="roi-bar">
      <div class="roi-title">Your ROI</div>
      <div class="roi-value">
        <span class="roi-icon">📈</span>
        <span>$9,600+ saved annually</span>
      </div>
      <div class="payback-period">⚡ Pays for itself in ~2 months</div>
    </div>

    <div class="trust-section">
      <div class="trust-label">Why clients choose us</div>
      <div class="trust-grid">
        <div class="trust-badge">
          <svg class="badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <div class="badge-text">
            <span class="badge-highlight">500+</span> returns filed
          </div>
        </div>

        <div class="trust-badge">
          <svg class="badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
          </svg>
          <div class="badge-text">
            QuickBooks ProAdvisors
          </div>
        </div>

        <div class="trust-badge">
          <svg class="badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M12 14l9-5-9-5-9 5 9 5z"/>
            <path d="M12 14l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14z"/>
          </svg>
          <div class="badge-text">
            Certified Tax Pros
          </div>
        </div>

        <div class="trust-badge">
          <svg class="badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
          </svg>
          <div class="badge-text">
            Cutting-edge strategy
          </div>
        </div>

        <div class="trust-badge">
          <svg class="badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/>
          </svg>
          <div class="badge-text">
            Tech stack experts
          </div>
        </div>

        <div class="trust-badge">
          <svg class="badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <div class="badge-text">
            Same-day response
          </div>
        </div>
      </div>
    </div>

    <div class="cta-section">
      <button class="cta-button">
        <span class="button-text">Secure Your Spot</span>
        <svg class="button-arrow" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
          <line x1="5" y1="12" x2="19" y2="12"></line>
          <polyline points="12 5 19 12 12 19"></polyline>
        </svg>
      </button>
      <p class="urgency-text">
        <span class="urgency-icon">🔥</span>
        <span>Only 3 spots left this month</span>
      </p>
      <p class="disclaimer">*Final pricing customized to your needs. No surprises, ever.</p>
    </div>
  </div>
</body>
</html>